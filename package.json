{
  "name": "jari-inna",
  "version": "0.1.0",
  "private": true,
  "description": "시티드(Seated) - 수도권 지하철 자리 안내 모바일 앱",
  "workspaces": [
    "server",
    "shared",
    "app"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:app": "npm run start --workspace=app",
    "build:server": "npm run build --workspace=server"
  },
  "engines": {
    "node": ">=18"
  }
}
