{
  "name": "jari-inna",
  "version": "0.1.0",
  "private": true,
  "description": "자리있나(JariInna) - 4호선 칸별 실시간 혼잡도 + 좌석 공유 모바일 앱",
  "workspaces": [
    "server",
    "shared",
    "app"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:app": "npm run start --workspace=app",
    "build:server": "npm run build --workspace=server"
  },
  "engines": {
    "node": ">=18"
  }
}
